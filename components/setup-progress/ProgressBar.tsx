import { View, Text } from "react-native";
import React from "react";

const ProgressBar = () => {
  return (
    <View>
      <Text className="text-red-500">ProgressBar</Text>
    </View>
  );
};

export default ProgressBar;
